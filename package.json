{
  "name": "clefkey-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Extension navigateur Clefkey — autofill, générateur et capture (client du coffre Clefkey).",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs --target chromium",
    "build:firefox": "node scripts/build.mjs --target firefox",
    "build:all": "node scripts/build.mjs --target all",
    "icons": "node scripts/build.mjs --target icons"
  },
  "devDependencies": {},
  "dependencies": {
    "@img/sharp-wasm32": "^0.35.3"
  }
}
